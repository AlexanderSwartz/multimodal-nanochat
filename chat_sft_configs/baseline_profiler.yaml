# Training Hyperparameters
train_vision_only: true
vision_lr: 1e-2
num_iterations: 30
total_batch_size: 32768
device_batch_size: 16
max_seq_len: 128
load_optimizer: 0

# Data & Model Pipeline
train_jsonl: "/home/as7629/multimodal-nanochat/COCO_data/coco_train.jsonl"
val_jsonl: "/home/as7629/multimodal-nanochat/COCO_data/coco_val.jsonl"
compute_embeddings: true
pin_memory: false

# Evaluation & Profiling
eval_every: 15
eval_tokens: 12800
profile: true

# Logging & Experiment Tracking (Weights & Biases)
wandb_group: "data_pipeline_tuning_online"
run: "baseline_online_w0_persistFalse_pinFalse_profileTrue"